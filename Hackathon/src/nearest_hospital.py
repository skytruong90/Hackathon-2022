import geocoder
import googlemaps


def hospital_finder():
    """
    # Disabling this actually, I don't want to run up my API calls.
    # In the real world, we'd use google maps, but it's too risky to share my api.
    gmaps = googlemaps.Client(key='')
    places_result = gmaps.places_nearby(location=(lat, lng), radius=1000, type='hospital')
    name = places_result['results'][0]['name']
    phone = places_result['results'][0]['formatted_phone_number']
    """



